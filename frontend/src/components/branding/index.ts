/**
 * EXPENSESINK Branding Components
 * Export barrel for all branding-related components
 */

export { default as ExpenseSinkLogo } from './ExpenseSinkLogo';

// Re-export types if needed
export type { ExpenseSinkLogoProps } from './ExpenseSinkLogo';
